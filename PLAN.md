
  # PLAN.md：微调 Agent 多选同类控件批量添加

  模型：GPT-5

  ## 目标与范围

  扩展 add_control：用户选中 2～20 个同类锚点后，一条指令可在每个锚点旁添加相同素材。

  保持以下行为不变：

  - 单选添加控件的全部逻辑和结果。
  - 移动、缩放、删除、对齐、标签等其他微调动作。
  - 现有 API 请求/响应结构。
  - Patch 的预览、接受、撤销和管线数据处理。
  - 不新增按钮、快捷入口或选择交互。

  主要修改：

  - model/refine_agent.py
  - tests/test_refine_add_control.py
  - web/scada/tests/refineStoreAddControl.test.ts

  ## 最终决策与实现契约

  ### 1. 动作及校验

  - add_control.target_ids 扩展为可包含当前全部选中锚点。
  - 后端按集合比较 target_ids 与当前选择，必须完全一致；不得缺少、增加或重复，模型返回顺序不影响结果。
  - 单选继续走现有执行路径，不改变错误、缩放、方位、命名、Patch 或消息行为。
  - 多选仅允许 2～20 个锚点；超过 20 个返回 HTTP 200、patch=[] 和确定性中文原因。
  - “同类”定义为所有锚点的 p.image 均为非空字符串且区分大小写完全相等。
  - 相同 displayName 但不同 p.image 视为异类；不同 displayName 但相同 p.image 允许批量操作。
  - 异类或缺少 p.image 时返回 HTTP 200、空 Patch，不产生任何节点。
  - 路由层现有重复 selected_node_ids 校验保持 HTTP 422；未知或不可编辑 ID 的现有 HTTP 422 行为也不改变。
  - 素材快照、素材歧义、方位缺失、非法方位以及 add_control 必须独占请求等现有规则保持不变。
  - 其他多选动作不执行同类校验，也不受 20 个锚点上限影响。

  ### 2. 批量生成规则

  - 选中 N 个锚点、指定一个方位时，每个锚点生成 1 个控件。
  - 指定左右或上下双侧时，每个锚点生成 2 个控件。
  - 新增控件全部使用同一素材和统一缩放比例。
  - 锚点按 json_data.d 中的出现顺序规划，不采用点击顺序。
  - 新节点 ID 从当前最大 i + 1 开始，按锚点画布顺序、再按规范化方位顺序连续分配。
  - 显示名称沿用现有全画布唯一命名规则。
  - 每个新节点分别继承对应锚点的 layout.group 和 layout.instance；缺失时沿用当前回退规则。
  - layout.materialName、源尺寸、资源路径及节点结构沿用现有单选生成规则。
  - 所有新增节点完成后只生成一次 contentRect 更新。
  - Patch 仍作为一个整体预览、接受或撤销，禁止部分提交。
  - 预览、接受、撤销后都保留原锚点选择；新增节点不自动选中。
  - 管线数据保持不变。

  ### 3. 放置与自动换向

  保留现有约束：

  - 与锚点保持 40px 间距。
  - 与其他已有控件及本批已规划节点保持至少 40px 间距。
  - 遵守画布和安全区域。
  - 缩放范围为 100%～50%，低于 50% 不允许。
  - 标签不新增为障碍物，保持当前单选语义。

  多选规划采用以下确定算法：

  1. 在 50% 最小允许尺寸下，按画布顺序为每个锚点确定方位。
  2. 优先尝试用户指定方位；只有该方位在 50% 时仍不可行才自动换向。
  3. 单侧回退顺序固定为：
      - 左：左 → 右 → 上 → 下
      - 右：右 → 左 → 上 → 下
      - 上：上 → 下 → 左 → 右
      - 下：下 → 上 → 左 → 右

  4. 左右双侧不可行时，该锚点整对旋转为上下；上下不可行时整对旋转为左右。不得拆成两个不成对方向。
  5. 换向只作用于失败锚点，其他锚点保留请求方向。
  6. 当前锚点自身不作为其新增节点的障碍；其他所有控件，包括其他已选锚点，均作为障碍。
  7. 已规划的新节点立即成为后续锚点的障碍，因此画布顺序靠前的锚点优先保留请求方向。
  8. 方位确定后，在不改变方位分配的前提下，从 100% 向 50% 搜索整批共同可用的最大缩放比例。
  9. 每个候选比例必须重新验证全部新增节点之间以及与原控件之间的间距。
  10. 任一锚点在全部允许方向或方位对中仍不可行时，整批返回空 Patch，不保留部分成功结果。
  11. 单选请求不得使用自动换向，仍按当前逻辑在指定方向失败。

  ### 4. 提示词与反馈

  - 更新 Agent 提示词中的 add_control 示例和规则，允许多选时返回全部当前选中 ID。
  - 明确禁止模型自行缩减目标集合、加入文字点名的未选控件或为不同锚点选择不同素材。
  - 后端负责最终确定性校验，不能依赖模型保证同类、数量或目标集合正确。
  - 批量成功消息至少包含：
      - 锚点数量。
      - 新增节点总数。
      - 素材名称。
      - 统一缩放比例。
      - 发生换向时，列出锚点 ID、原请求方向和最终方向。

  - 无换向时明确说明全部按请求方向放置。
  - 失败消息说明没有生成任何节点，并给出失败类型；空间失败应列出无法安排的锚点 ID。
  - 不输出逐节点坐标，避免大批量消息不可读。

  ## 验收标准与测试

  ### 后端功能

  - 两个相同非空 p.image 的锚点单侧添加，生成 2 个节点。
  - N 个锚点双侧添加，生成 2N 个节点。
  - 不同显示名但相同资源路径可成功。
  - 相同显示名但不同资源路径返回空 Patch。
  - 任一锚点资源路径缺失或为空时返回空 Patch。
  - 20 个锚点可以执行；21 个锚点返回空 Patch。
  - 模型返回目标子集、超集或非选中 ID 时返回空 Patch。
  - 模型返回目标顺序与选择顺序不同，但集合一致时成功，并按画布顺序生成。
  - 所有新增节点使用相同缩放比例。
  - 请求方向在 50% 可行时不得为了更大尺寸换向。
  - 单侧空间不足时按固定顺序回退，且只改变失败锚点。
  - 左右失败时整对旋转为上下；上下失败时整对旋转为左右。
  - 批量新增节点之间满足 40px 间距，后续锚点遵循画布顺序避让。
  - 所有回退方向仍不可行时整批为空，不出现部分节点。
  - 新节点 ID、唯一名称、元数据继承和单次 contentRect 更新具有确定性。
  - 成功消息包含数量、素材、统一缩放和换向明细。
  - 重复请求 ID 继续返回 HTTP 422。

  ### 兼容性

  - 现有单选添加控件测试全部原样通过。
  - 单选空间不足时仍失败，不自动换向。
  - 单选生成的节点结构、尺寸、坐标、命名和消息不变。
  - 移动、缩放、删除、对齐、分布、交换、标签等测试不受影响。
  - API 字段保持 selected_node_ids、patch、message，不增加新字段。

  ### 前端状态

  - 批量 Patch 能追加多个 /d/- 节点。
  - Patch 预览后保留全部原锚点选择。
  - 接受后保留新增节点和原锚点选择。
  - 撤销后恢复原 JSON、原管线和原锚点选择。
  - 批量新增不修改管线数据。
  - 不新增或修改微调 Agent 的按钮、快捷指令和多选方式。

  ### 验证命令

  - 使用 Python 3.9。
  - 运行添加控件及路由相关测试。
  - 运行完整 Python 测试集。
  - 按项目要求运行 ruff check .。
  - 在 web/scada 运行前端测试、lint 和 build。
  - 实现代码不包含注释。

  ## 已知风险与接受的限制

  - 资源路径采用原始字符串完全相等；语义相同但写法不同的路径会被拒绝，这是已接受的确定性取舍。
  - 画布顺序优先是贪心且确定的，不保证找到全局最美观布局。
  - 同一批锚点可能因局部空间不同而落在不同方向；成功消息必须显式披露。
  - 几何规划需检查新增节点之间的碰撞；只复用当前单锚点算法并独立计算会产生重叠，不可接受。
  - 最多 20 个锚点将最坏新增量限制为 40 个节点，避免规划、Patch 预览和浏览器状态更新失控。
  - 不涉及数据迁移、API 版本升级或功能开关。