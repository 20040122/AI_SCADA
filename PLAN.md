
  # PLAN.md — DaoSCADA 控件宽高比一致性修复

  ## 1. 结论与验收目标

  根因已确认：

  - 布局算法在素材比例无法同时满足角色宽高上下限时，回退到固定 preferred_w × preferred_h，破坏素材原始比例。
  - 例如液压泵从素材 154×70 变为 160×240，最终输出 111.8×167.7。
  - 本地预览通过 object-fit: contain 和内边距隐藏了错误包围盒；DaoSCADA 按节点宽高拉伸，因此插入后才出现压扁或拉长。
  - 当前素材中冷水机、油箱、电机、液压泵、吸油过滤器、回油单向阀均可复现比例漂移。

  必须达到：

  - 所有可编辑控件在生成、拖拽缩放、AI 微调和插入阶段始终保持素材库宽高比。
  - 输出比例相对素材比例误差不超过 0.1%。
  - 背景、标题栏、标题文字及现有 root 角色分类行为不变。
  - 历史未插入 JSON 在插入前自动纠正，不引入新的重叠。
  - 本地预览严格模拟 DaoSCADA 拉伸语义，不再掩盖错误。
  - 素材尺寸缺失或身份存在歧义时阻止插入并列出具体控件。

  本次不做：

  - 不重新设计“分组根节点一律使用 root 尺寸”的角色规则。
  - 不提供自由变形或比例解锁。
  - 不在线读取远端资源尺寸兜底。
  - 不批量迁移已经上传到 DaoSCADA 的旧画面。

  ## 2. 统一尺寸规则

  建立唯一的控件几何模块，由布局、微调和插入流程共同调用。

  ### 新布局尺寸

  以素材库 width/height 为比例真值，对角色范围只使用统一缩放系数：

  - minScale = max(minW/sourceW, minH/sourceH)
  - maxScale = min(maxW/sourceW, maxH/sourceH)
  - 范围可满足时，保持现有策略：尽量使用原始尺寸，并在必要时统一放大或缩小到合法范围。
  - 范围不可同时满足时，使用 maxScale，得到最大且不突破角色最大边界的等比尺寸；允许其中一边低于最小值。
  - 放入布局槽位时继续只使用统一缩放系数。
  - 最终宽高保留两位小数，并通过比例校验后输出。

  每个生成控件在 a 中记录内部元数据：

  - layout.materialName
  - layout.sourceWidth
  - layout.sourceHeight

  这些字段用于后续微调和插入校验，避免仅靠可能重复的图片路径反查素材。

  ### 微调尺寸

  - 拖拽手柄时，以素材比例把用户拖出的矩形视为上限框，取框内最大的等比尺寸，并保持对角锚点不动。
  - AI 使用 scale 时同时缩放宽高。
  - AI 只指定宽或高时，由该边和素材比例推导另一边。
  - AI 指定不合比例的 width × height 时，将其视为上限框并执行等比内接。
  - 所有 resize Patch 必须同时输出最终 width 和 height。
  - 画布边界裁剪只能缩小统一比例，禁止分别裁剪宽高。
  - 控件关联标签应根据纠正后的控件边界重新计算位置。
  - 比例不合法的 Patch 不进入待接受状态。

  ### 历史 JSON 纠偏

  插入前按以下顺序解析比例真值：

  1. 使用节点内的 layout.sourceWidth/sourceHeight。
  2. 缺失时，按素材名称与资源路径联合匹配素材库。
  3. 对自动追加序号的实例名称，可移除实例序号后匹配，但候选必须只有一个宽高比。
  4. 无匹配、尺寸非正数或候选比例不唯一时返回 422，禁止上传。

  纠偏采用内接策略：

  - 保持节点中心坐标不变。
  - 将素材比例内接到当前节点包围盒。
  - 只缩小其中一边，不扩大当前宽或高，因此不会制造新的重叠。
  - 更新关联标签和 contentRect。
  - 比较纠偏前后的碰撞集合；不得新增或扩大碰撞，已有碰撞作为警告返回。

  ## 3. 插入接口与前端行为

  新增后端统一插入网关：

  POST /api/canvas/upload

  请求：

  {
    "file_name": "画面.json",
    "json_data": {}
  }

  成功响应：

  {
    "file_name": "画面.json",
    "json_data": {},
    "corrections": [
      {
        "node_i": 1,
        "display_name": "液压泵",
        "image": "assets/现场控制系统/设备图片/液压泵.png",
        "before": {"width": 111.8, "height": 167.7},
        "after": {"width": 111.8, "height": 50.82}
      }
    ],
    "warnings": []
  }

  后端行为：

  - 校验文件名为不含路径的 .json 文件。
  - 复制输入 JSON，完成素材解析、比例纠偏、碰撞检查和 schema 校验。
  - 使用配置项保存 DaoSCADA 上传地址和目标目录，不再由前端硬编码。
  - 通过异步 HTTP 客户端向 displays/dutzcm/{file_name} 发送 multipart 上传。
  - 素材或 JSON 问题返回 422；DaoSCADA 拒绝或连接失败返回 502；超时返回 504。
  - 后端可直接访问 daoscada.local，当前上传接口无需浏览器登录态。

  布局 Agent 与微调 Agent 的插入按钮统一调用该接口。上传成功后：

  - 用响应中的规范化 JSON 回写发起上传的 store。
  - 重新提取节点和装饰元素，使画布立即展示远端实际几何。
  - 显示修正数量，并提供逐控件修正明细。
  - 微调存在未接受 Patch 时继续禁止插入。

  本地预览调整为远端仿真：

  - 控件图片移除 contain 和额外 padding，按节点宽高完整拉伸。
  - 装饰图片采用与远端一致的填充行为。
  - 正确 JSON 仍保持素材比例；错误 JSON 会在插入前直接暴露变形。

  ## 4. 测试与验收

  后端单元测试：

  - 覆盖尺寸约束可满足和不可满足两条分支。
  - 对冷水机、油箱、电机、液压泵、吸油过滤器、回油单向阀验证输出比例误差不超过 0.1%。
  - 验证历史纠偏保持中心、不扩大包围盒、不新增碰撞。
  - 验证嵌入元数据、素材库回退、重复图片路径、缺失尺寸和歧义阻断。
  - 验证 AI 的倍率、单边尺寸和不合比例目标框均输出等比 Patch。
  - Mock DaoSCADA 上传，验证 multipart 路径、规范化内容及 422/502/504 错误映射。

  前端测试：

  - 拖动四角和四边手柄后比例保持不变，对角锚点稳定。
  - 插入成功后布局 store、微调 store 分别正确回写规范化 JSON。
  - 修正明细正确显示，失败时不显示“已插入系统”。
  - 预览不再使用会掩盖比例错误的 contain/padding。

  集成验收：

  - 在 1920×1080 画布生成包含上述六类问题控件的布局。
  - 布局 Agent、微调 Agent 和 DaoSCADA 三处视觉比例一致。
  - 背景、标题栏及标题文字的尺寸和位置与修复前一致。
  - 对控件执行放大、指定尺寸、拖拽缩放后重新插入，远端不发生拉伸。
  - 素材尺寸缺失时上传被阻止，并显示控件 ID、名称和资源路径。

  实施完成后运行：

  - ruff check .
  - pytest
  - npm test
  - npm run lint
  - npm run build

  实现代码保持 Python 3.9 兼容且不添加代码注释。